program miprograma
Var
    dato, var2, dat6: integer;
    dato5:char;
    dato, var2: integer;
    dato5:char;
Begin
    if a < 20 then
        dato := 10;
        {Este es un comentario jeje
         viva argentina
        
        }
    else if a < 30 then
        dato := 20;
    else
        write("hola", brandon);
        read(dato);

    write(brandon);

    for i:= 1 to 10 d
        if a < 20 then
            for i:= 1 to 10 do
                write("hola", brandon);
                
        else
            for i:= 1 to 10 do
                write("hola", brandon);

    writeln(i);
    
    while i <= 5 do Begin
         if a < 20 then
            for i:= 1 to 10 do
                write("hola", brandon);
                {comentario de linea}
        writeln(i);
        i :=1;
    End;


!!!
    repeat
      writeln("value of a: ", a);
      {prueba
      de 
      otro
      comentario
      }
      a:=1;
    until a = 20;

    i := 4 + 1; 
    i := 5 + (7*1)-bra; 
   
End

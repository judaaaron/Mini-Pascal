program miprograma;
Var
    dato, var2, dat6: integer;
    dato5:char;
    dato, var2: integer;
    dato5:char;
Begin
    
    if a < 20 then
        dato := 10;
        {hola braulio ESTE ES UN COMENTARIO}
    else if a < 30 then
        dato := 20;
    else
        write("hola", brandon);
        read(dato);

    write(brandon);

    for i:= 1 to 10 do
        if a < 20 then
            for i:= 1 to 10 do
                write("hola", brandon);
                
        else
            for i:= 1 to 10 do
                write("hola", brandon);

    writeln(i);
    
    while i <= 5 do Begin
         if a < 20 then
            for i:= 1 to 10 do
                write("hola", brandon);
                {hola braulio ESTE ES UN COMENTARIO}
        writeln(i);
        i :=1;
    End;

    repeat
      writeln("value of a: ", a);
      {hola braulio ESTE ES UN COMENTARIO}
      a:=1;
    until a = 20;

    i := 4 + 1; 
    i := 5 + (7*1)-bra; 
   
End
